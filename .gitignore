# dependencias
node_modules/

# binarios pesados / con copyright — NUNCA van a git, viven solo en R2
apps/*/assets/

# artefactos generados
apps/*/dist/

# estado de deploy — vive en R2 (_state/), NO en git
apps/*/registry.json
apps/*/channels.lock.json

# credenciales
.env
.env.local

# basura del SO / editor
.DS_Store
Thumbs.db
*.log

# paquete de trabajo local para la revisión de WoH (no es parte del repo)
woh-review-package/
